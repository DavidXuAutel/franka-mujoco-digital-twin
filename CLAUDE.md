# franka-mujoco-digital-twin

## Prior Cursor history

Migrated from Cursor `state.vscdb` on 2026-07-28: **29 conversations, 336 messages** at `~/.cursor-history/franka-mujoco-digital-twin/cursor-history.md`. Also has a worktree `feature-online-digital-twin-mvp` (was a separate Cursor workspace but had 0 chat messages). Grep or read that file when you need context on past decisions.
